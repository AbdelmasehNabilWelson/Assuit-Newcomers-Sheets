fun main() {
    val (a, b) = readln().split(" ")
    if (a.toInt() % b.toInt() == 0 || b.toInt() % a.toInt() == 0) {
        println("Multiples")
    } else {
        println("No Multiples")
    }
}
